@page "/auth"

@code {

    protected override async Task OnInitializedAsync()
    {
        try
        {
            var response = await AuthService.SetUpAsync();
            
            
        }
        catch (Exception e)
        {
            Console.WriteLine(e);
            throw;
        }
    }
}